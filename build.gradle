apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'idea'

group   = 'andreassimon'
version = '0.1.2-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()

    compile group: "com.googlecode.flyway", name: "flyway-ant", version: "1.5"
    compile group: "mysql", name: "mysql-connector-java", version: "5.0.5"

    groovy 'org.codehaus.groovy:groovy:1.8.0'
}
