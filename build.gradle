apply plugin: 'groovy'
apply plugin: 'maven'

group   = 'de.quagilis'
version = '0.1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    groovy 'org.codehaus.groovy:groovy:1.8.0'

    flyway group: "com.googlecode.flyway", name: "flyway-ant", version: "1.5"
    flyway group: "mysql", name: "mysql-connector-java", version: "5.0.5"
}
